//it injects the script inside the page code, isn't is cool?
//very few access to chrome apis, like message passing
// alert("This is content_script.  it injects the script inside the page code");
//it runs just after when webpage is loaded

console.log('content script running');
